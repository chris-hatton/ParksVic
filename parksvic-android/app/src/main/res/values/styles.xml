<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SiteDetailTitleLabel" parent="TextAppearance.AppCompat.Title">
        <item name="android:layout_columnWeight">0.0</item>
        <item name="android:layout_marginRight">@dimen/gap_medium</item>
        <item name="android:layout_gravity">right</item>
    </style>

    <style name="SiteDetailValueLabel" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_columnWeight">1.0</item>
        <item name="android:layout_gravity">left</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
